# AndroidX is required by the app's AndroidX/Compose dependencies.
android.useAndroidX=true
# Keep Gradle memory bounded for CI and typical 6 GB RAM development machines.
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
